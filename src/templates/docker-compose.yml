services:
  delega:
    image: ghcr.io/delega-dev/delega:__DELEGA_VERSION__
    restart: unless-stopped
    ports:
      - "__DELEGA_PORT__:18890"
    volumes:
      - delega-data:/app/.delega

volumes:
  delega-data:
