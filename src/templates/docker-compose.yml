services:
  delega:
    image: ghcr.io/delega-dev/delega:__DELEGA_VERSION__
    restart: unless-stopped
    ports:
      # Change 127.0.0.1 to 0.0.0.0 only if you need network exposure
      - "127.0.0.1:__DELEGA_PORT__:18890"
    volumes:
      - delega-data:/app/.delega

volumes:
  delega-data:
